#include "vol_mongo_info.h"

void* vol_mongo_info_copy(const void* info) {
  lwlog_trace();
  UNIMPLEMENTED_ERROR();
  return (void*)1;
}

herr_t vol_mongo_info_cmp(int* cmp_value, const void* info1,
                          const void* info2) {
  lwlog_trace();
  UNIMPLEMENTED_ERROR();
  return NULL;
}

herr_t vol_mongo_info_free(void* info) {
  lwlog_trace();
  UNIMPLEMENTED_ERROR();
  return NULL;
}

herr_t vol_mongo_info_to_str(const void* info, char** str) {
  lwlog_trace();
  UNIMPLEMENTED_ERROR();
  return NULL;
}

herr_t vol_mongo_info_from_str(const char* str, void** info) {
  lwlog_trace();
  UNIMPLEMENTED_ERROR();
  return NULL;
}